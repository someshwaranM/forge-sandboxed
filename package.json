{
  "name": "eyewitness",
  "private": true,
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace storefront",
    "build": "npm run build --workspace storefront",
    "start": "npm run start --workspace storefront",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  }
}
